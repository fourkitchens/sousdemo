{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}

{{ attach_library('sousdemo/breadcrumbs') }}

{% include "@organisms/navigation/breadcrumbs/breadcrumbs.twig" with {
  breadcrumbs_items: breadcrumb,
}%}
